const MAX_INI = 100;
const MIN_INI = 0;
const AVG_INI = (MAX_INI - MIN_INI) / 2;
const MAX_AGI = 100;
const MIN_AGI = 0;
const AVG_AGI = (MAX_AGI - MIN_AGI) / 2;
const MAX_WSD = 100;
const MIN_WSD = 0;
const AVG_WSD = (MAX_WSD - MIN_WSD) / 2;
const CAR_MULT = 2;
const PLU_MULT = 4;
const RES_MULT = 0.01;

export default {
  MAX_INI,
  MIN_INI,
  AVG_INI,
  MAX_AGI,
  MIN_AGI,
  AVG_AGI,
  MAX_WSD,
  MIN_WSD,
  AVG_WSD,
  CAR_MULT,
  PLU_MULT,
  RES_MULT,
};
